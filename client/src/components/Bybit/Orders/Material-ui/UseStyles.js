// material-ui
import { makeStyles } from "@material-ui/core/styles";

const UseStyles = makeStyles(() => ({
  root: {
    width: "100%",
  },
  container: {
    maxHeight: 750,
  },
}));

export default UseStyles;
