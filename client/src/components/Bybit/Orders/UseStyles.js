// material-ui
import { makeStyles } from "@material-ui/core/styles";

const UseStyles = makeStyles(() => ({
  root: {
    width: "100%",
  },
  container: {
    maxHeight: 1000,
  },
}));

export default UseStyles;
